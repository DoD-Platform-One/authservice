#########################
# Disable Core Packages #
#########################
bbctl:
  enabled: false
monitoring:
  enabled: false
grafana:
  enabled: false
neuvector:
  enabled: false
loki:
  enabled: false
tempo:
  enabled: false
kyvernoReporter:
  enabled: false
kiali:
  enabled: false
addons:
  minioOperator:
    enabled: false
########################
# Required for package #
########################
  authservice:
    enabled: true
    # Temporary redis passthrough values added for the upgrade pipeline job to pass, 
    # they will be removed once the BB umbrella test-values.yaml are updated.
    values:
      redis-bb:
        upstream: 
          master:
            persistence:
              size: 256Mi
          replica:
            persistence:
              size: 256Mi
            
kyvernoPolicies:
  values:
    bbtests:
      enabled: false
kyverno:
  values:
    bbtests:
      enabled: false
